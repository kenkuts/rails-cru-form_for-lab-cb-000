<h1>List of Genres</h1>
<br>
<table style="width:50%" border="solid">
    <tr>
        <th>Genres</th>
    </tr>
    <% @genres.each do |genre| %>
    <tr>
        <td><%= link_to genre.name, genre_path(genre) %></td>
    </tr>
    <% end %>
</table>
<br>
<br>
<%= button_to "Add New Genre", new_genre_path, html_options = { :method => :get} %>
<%= button_to "List Artists", artists_path, html_options = { :method => :get} %>
<%= button_to "List Songs", songs_path, html_options = { :method => :get} %>